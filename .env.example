# ===========================================
# KITCHAI - CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ===========================================

# ===========================================
# DATABASE CONFIGURATION (Turso DB)
# ===========================================
# URL de conexión a tu base de datos Turso
# Puedes usar https:// o libsql:// según tu configuración
TURSO_DATABASE_URL=https://your-database.turso.io
# o TURSO_DATABASE_URL=libsql://your-database.turso.io

# Token de autenticación de Turso DB
# Obtén este token desde tu dashboard de Turso
TURSO_AUTH_TOKEN=your_turso_auth_token_here

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
# Entorno de ejecución: development o production
ENVIRONMENT=development

# ===========================================
# JWT (JSON WEB TOKEN) CONFIGURATION
# ===========================================
# Clave secreta para firmar los tokens JWT
# ⚠️  IMPORTANTE: Usa una clave segura de al menos 32 caracteres
# ⚠️  NUNCA compartas esta clave ni la subas a repositorios públicos
# ⚠️  En producción, esta variable es OBLIGATORIA
# Puedes generar una con: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Algoritmo de firma JWT (recomendado: HS256)
JWT_ALGORITHM=HS256

# Tiempo de expiración del token en minutos (60 = 1 hora)
JWT_EXPIRATION_MINUTES=60

# ===========================================
# NOTAS DE SEGURIDAD
# ===========================================
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Nunca subas el archivo .env a git (ya está en .gitignore)
# 3. Usa claves diferentes en desarrollo y producción
# 4. Cambia JWT_SECRET_KEY regularmente en producción
# 5. Mantén tus credenciales seguras y privadas
